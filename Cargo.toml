#:schema https://www.schemastore.org/cargo.json

[workspace]
resolver = "3"
members = ["crate", "test", "bench"]

[workspace.dependencies]

# Workspace
oxc_estree_compat = { version = "0.1.0", path = "crate" }

# Crates
oxc = { version = "~0.146.0" }
sonic-rs = { version = "~0.5.0" }
