#:schema https://www.schemastore.org/cargo.json

[package]
name = "test"
edition = "2024"
rust-version = "1.96.0"
publish = false

[dependencies]
oxc = { workspace = true, features = ["codegen"] }
oxc_estree_compat = { workspace = true }
sonic-rs = { workspace = true }
